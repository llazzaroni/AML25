# AML_projects

All the files named with caps lock .py are files for finding the best hyperparameters for the specific models. stack.py performs cross validation on all the models put together with the stacking regressor. main.py performs the final predictions on the test set.
